{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for amazon clone but as a alternative for firebase cloud function",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "nodemon": "^2.0.15",
    "stripe": "^8.203.0"
  }
}
